Директория со всякими полезными скриптами.
support.py - умеет перегонять данные из collection.db в mysql (адрес localhost) и обратно;
             умеет удалять записи - дубликаты;
             считает количество записей и количество уникальных записей (и сравнивает с предыдущими значениями
                                                                         (это в файле history))
              СИНТАКСИС:
                       флаги в порядке их следования при вызове
                        -h --help: описание (то же, что я пишу здесь);
                        -b --back: Если установлен гонит из MySQL в SQLite3 (collection.db), в противном случае
                                                из collection.db в mysql (default: False);
                        -f --filter: Гнать всё или только уникальные (по умолчанию он гонит только уникальные,
                                       но если поставить этот флаг - то гонит всё) (default: True);
                        -n --new: Создание новой бд collection_temp.db и перегон данных в неё. Не дописано.
                                                                                    (default: False);
                        -c --check: Анализ общего количества записей и уникальных записей в MySQL. Если установлен,
                                        остальные флаги игнорируются (default: False)